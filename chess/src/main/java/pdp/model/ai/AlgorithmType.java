package pdp.model.ai;

/** Enum corresponding to the different types of AI algorithms. */
public enum AlgorithmType {
  MINIMAX,
  ALPHA_BETA,
  ALPHA_BETA_PARALLEL,
  ALPHA_BETA_ID,
  ALPHA_BETA_ID_PARALLEL,
  MCTS;
}
